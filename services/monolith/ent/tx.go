// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"sync"

	"entgo.io/ent/dialect"
)

// Tx is a transactional client that is created by calling Client.Tx().
type Tx struct {
	config
	// Article is the client for interacting with the Article builders.
	Article *ArticleClient
	// ArticleTagsTag is the client for interacting with the ArticleTagsTag builders.
	ArticleTagsTag *ArticleTagsTagClient
	// Category is the client for interacting with the Category builders.
	Category *CategoryClient
	// Collect is the client for interacting with the Collect builders.
	Collect *CollectClient
	// Comment is the client for interacting with the Comment builders.
	Comment *CommentClient
	// Dept is the client for interacting with the Dept builders.
	Dept *DeptClient
	// File is the client for interacting with the File builders.
	File *FileClient
	// KnowledgeChunk is the client for interacting with the KnowledgeChunk builders.
	KnowledgeChunk *KnowledgeChunkClient
	// Like is the client for interacting with the Like builders.
	Like *LikeClient
	// Link is the client for interacting with the Link builders.
	Link *LinkClient
	// Menu is the client for interacting with the Menu builders.
	Menu *MenuClient
	// Msgboard is the client for interacting with the Msgboard builders.
	Msgboard *MsgboardClient
	// MyFile is the client for interacting with the MyFile builders.
	MyFile *MyFileClient
	// OperationLog is the client for interacting with the OperationLog builders.
	OperationLog *OperationLogClient
	// PayOrder is the client for interacting with the PayOrder builders.
	PayOrder *PayOrderClient
	// Privilege is the client for interacting with the Privilege builders.
	Privilege *PrivilegeClient
	// RagIndexJob is the client for interacting with the RagIndexJob builders.
	RagIndexJob *RagIndexJobClient
	// RagQueryLog is the client for interacting with the RagQueryLog builders.
	RagQueryLog *RagQueryLogClient
	// Reply is the client for interacting with the Reply builders.
	Reply *ReplyClient
	// Role is the client for interacting with the Role builders.
	Role *RoleClient
	// RoleDataScope is the client for interacting with the RoleDataScope builders.
	RoleDataScope *RoleDataScopeClient
	// RoleMenusMenu is the client for interacting with the RoleMenusMenu builders.
	RoleMenusMenu *RoleMenusMenuClient
	// RolePrivilegesPrivilege is the client for interacting with the RolePrivilegesPrivilege builders.
	RolePrivilegesPrivilege *RolePrivilegesPrivilegeClient
	// RoleUsersUser is the client for interacting with the RoleUsersUser builders.
	RoleUsersUser *RoleUsersUserClient
	// Rpg is the client for interacting with the Rpg builders.
	Rpg *RpgClient
	// RpgActivity is the client for interacting with the RpgActivity builders.
	RpgActivity *RpgActivityClient
	// RpgArticleTip is the client for interacting with the RpgArticleTip builders.
	RpgArticleTip *RpgArticleTipClient
	// RpgGuild is the client for interacting with the RpgGuild builders.
	RpgGuild *RpgGuildClient
	// RpgItemConfig is the client for interacting with the RpgItemConfig builders.
	RpgItemConfig *RpgItemConfigClient
	// RpgLeaderboardSnapshot is the client for interacting with the RpgLeaderboardSnapshot builders.
	RpgLeaderboardSnapshot *RpgLeaderboardSnapshotClient
	// RpgLevelReward is the client for interacting with the RpgLevelReward builders.
	RpgLevelReward *RpgLevelRewardClient
	// RpgLotteryPool is the client for interacting with the RpgLotteryPool builders.
	RpgLotteryPool *RpgLotteryPoolClient
	// RpgQuest is the client for interacting with the RpgQuest builders.
	RpgQuest *RpgQuestClient
	// RpgUserAchievement is the client for interacting with the RpgUserAchievement builders.
	RpgUserAchievement *RpgUserAchievementClient
	// RpgUserBuff is the client for interacting with the RpgUserBuff builders.
	RpgUserBuff *RpgUserBuffClient
	// RpgUserGuildMember is the client for interacting with the RpgUserGuildMember builders.
	RpgUserGuildMember *RpgUserGuildMemberClient
	// RpgUserInventory is the client for interacting with the RpgUserInventory builders.
	RpgUserInventory *RpgUserInventoryClient
	// RpgUserLoadout is the client for interacting with the RpgUserLoadout builders.
	RpgUserLoadout *RpgUserLoadoutClient
	// RpgUserLotteryRecord is the client for interacting with the RpgUserLotteryRecord builders.
	RpgUserLotteryRecord *RpgUserLotteryRecordClient
	// RpgUserPet is the client for interacting with the RpgUserPet builders.
	RpgUserPet *RpgUserPetClient
	// RpgUserQuestProgress is the client for interacting with the RpgUserQuestProgress builders.
	RpgUserQuestProgress *RpgUserQuestProgressClient
	// RpgUserSocialLog is the client for interacting with the RpgUserSocialLog builders.
	RpgUserSocialLog *RpgUserSocialLogClient
	// ScheduledTask is the client for interacting with the ScheduledTask builders.
	ScheduledTask *ScheduledTaskClient
	// ScheduledTaskLog is the client for interacting with the ScheduledTaskLog builders.
	ScheduledTaskLog *ScheduledTaskLogClient
	// SensitiveWord is the client for interacting with the SensitiveWord builders.
	SensitiveWord *SensitiveWordClient
	// SensitiveWordHit is the client for interacting with the SensitiveWordHit builders.
	SensitiveWordHit *SensitiveWordHitClient
	// SiteNotification is the client for interacting with the SiteNotification builders.
	SiteNotification *SiteNotificationClient
	// Tag is the client for interacting with the Tag builders.
	Tag *TagClient
	// User is the client for interacting with the User builders.
	User *UserClient

	// lazily loaded.
	client     *Client
	clientOnce sync.Once
	// ctx lives for the life of the transaction. It is
	// the same context used by the underlying connection.
	ctx context.Context
}

type (
	// Committer is the interface that wraps the Commit method.
	Committer interface {
		Commit(context.Context, *Tx) error
	}

	// The CommitFunc type is an adapter to allow the use of ordinary
	// function as a Committer. If f is a function with the appropriate
	// signature, CommitFunc(f) is a Committer that calls f.
	CommitFunc func(context.Context, *Tx) error

	// CommitHook defines the "commit middleware". A function that gets a Committer
	// and returns a Committer. For example:
	//
	//	hook := func(next ent.Committer) ent.Committer {
	//		return ent.CommitFunc(func(ctx context.Context, tx *ent.Tx) error {
	//			// Do some stuff before.
	//			if err := next.Commit(ctx, tx); err != nil {
	//				return err
	//			}
	//			// Do some stuff after.
	//			return nil
	//		})
	//	}
	//
	CommitHook func(Committer) Committer
)

// Commit calls f(ctx, m).
func (f CommitFunc) Commit(ctx context.Context, tx *Tx) error {
	return f(ctx, tx)
}

// Commit commits the transaction.
func (tx *Tx) Commit() error {
	txDriver := tx.config.driver.(*txDriver)
	var fn Committer = CommitFunc(func(context.Context, *Tx) error {
		return txDriver.tx.Commit()
	})
	txDriver.mu.Lock()
	hooks := append([]CommitHook(nil), txDriver.onCommit...)
	txDriver.mu.Unlock()
	for i := len(hooks) - 1; i >= 0; i-- {
		fn = hooks[i](fn)
	}
	return fn.Commit(tx.ctx, tx)
}

// OnCommit adds a hook to call on commit.
func (tx *Tx) OnCommit(f CommitHook) {
	txDriver := tx.config.driver.(*txDriver)
	txDriver.mu.Lock()
	txDriver.onCommit = append(txDriver.onCommit, f)
	txDriver.mu.Unlock()
}

type (
	// Rollbacker is the interface that wraps the Rollback method.
	Rollbacker interface {
		Rollback(context.Context, *Tx) error
	}

	// The RollbackFunc type is an adapter to allow the use of ordinary
	// function as a Rollbacker. If f is a function with the appropriate
	// signature, RollbackFunc(f) is a Rollbacker that calls f.
	RollbackFunc func(context.Context, *Tx) error

	// RollbackHook defines the "rollback middleware". A function that gets a Rollbacker
	// and returns a Rollbacker. For example:
	//
	//	hook := func(next ent.Rollbacker) ent.Rollbacker {
	//		return ent.RollbackFunc(func(ctx context.Context, tx *ent.Tx) error {
	//			// Do some stuff before.
	//			if err := next.Rollback(ctx, tx); err != nil {
	//				return err
	//			}
	//			// Do some stuff after.
	//			return nil
	//		})
	//	}
	//
	RollbackHook func(Rollbacker) Rollbacker
)

// Rollback calls f(ctx, m).
func (f RollbackFunc) Rollback(ctx context.Context, tx *Tx) error {
	return f(ctx, tx)
}

// Rollback rollbacks the transaction.
func (tx *Tx) Rollback() error {
	txDriver := tx.config.driver.(*txDriver)
	var fn Rollbacker = RollbackFunc(func(context.Context, *Tx) error {
		return txDriver.tx.Rollback()
	})
	txDriver.mu.Lock()
	hooks := append([]RollbackHook(nil), txDriver.onRollback...)
	txDriver.mu.Unlock()
	for i := len(hooks) - 1; i >= 0; i-- {
		fn = hooks[i](fn)
	}
	return fn.Rollback(tx.ctx, tx)
}

// OnRollback adds a hook to call on rollback.
func (tx *Tx) OnRollback(f RollbackHook) {
	txDriver := tx.config.driver.(*txDriver)
	txDriver.mu.Lock()
	txDriver.onRollback = append(txDriver.onRollback, f)
	txDriver.mu.Unlock()
}

// Client returns a Client that binds to current transaction.
func (tx *Tx) Client() *Client {
	tx.clientOnce.Do(func() {
		tx.client = &Client{config: tx.config}
		tx.client.init()
	})
	return tx.client
}

func (tx *Tx) init() {
	tx.Article = NewArticleClient(tx.config)
	tx.ArticleTagsTag = NewArticleTagsTagClient(tx.config)
	tx.Category = NewCategoryClient(tx.config)
	tx.Collect = NewCollectClient(tx.config)
	tx.Comment = NewCommentClient(tx.config)
	tx.Dept = NewDeptClient(tx.config)
	tx.File = NewFileClient(tx.config)
	tx.KnowledgeChunk = NewKnowledgeChunkClient(tx.config)
	tx.Like = NewLikeClient(tx.config)
	tx.Link = NewLinkClient(tx.config)
	tx.Menu = NewMenuClient(tx.config)
	tx.Msgboard = NewMsgboardClient(tx.config)
	tx.MyFile = NewMyFileClient(tx.config)
	tx.OperationLog = NewOperationLogClient(tx.config)
	tx.PayOrder = NewPayOrderClient(tx.config)
	tx.Privilege = NewPrivilegeClient(tx.config)
	tx.RagIndexJob = NewRagIndexJobClient(tx.config)
	tx.RagQueryLog = NewRagQueryLogClient(tx.config)
	tx.Reply = NewReplyClient(tx.config)
	tx.Role = NewRoleClient(tx.config)
	tx.RoleDataScope = NewRoleDataScopeClient(tx.config)
	tx.RoleMenusMenu = NewRoleMenusMenuClient(tx.config)
	tx.RolePrivilegesPrivilege = NewRolePrivilegesPrivilegeClient(tx.config)
	tx.RoleUsersUser = NewRoleUsersUserClient(tx.config)
	tx.Rpg = NewRpgClient(tx.config)
	tx.RpgActivity = NewRpgActivityClient(tx.config)
	tx.RpgArticleTip = NewRpgArticleTipClient(tx.config)
	tx.RpgGuild = NewRpgGuildClient(tx.config)
	tx.RpgItemConfig = NewRpgItemConfigClient(tx.config)
	tx.RpgLeaderboardSnapshot = NewRpgLeaderboardSnapshotClient(tx.config)
	tx.RpgLevelReward = NewRpgLevelRewardClient(tx.config)
	tx.RpgLotteryPool = NewRpgLotteryPoolClient(tx.config)
	tx.RpgQuest = NewRpgQuestClient(tx.config)
	tx.RpgUserAchievement = NewRpgUserAchievementClient(tx.config)
	tx.RpgUserBuff = NewRpgUserBuffClient(tx.config)
	tx.RpgUserGuildMember = NewRpgUserGuildMemberClient(tx.config)
	tx.RpgUserInventory = NewRpgUserInventoryClient(tx.config)
	tx.RpgUserLoadout = NewRpgUserLoadoutClient(tx.config)
	tx.RpgUserLotteryRecord = NewRpgUserLotteryRecordClient(tx.config)
	tx.RpgUserPet = NewRpgUserPetClient(tx.config)
	tx.RpgUserQuestProgress = NewRpgUserQuestProgressClient(tx.config)
	tx.RpgUserSocialLog = NewRpgUserSocialLogClient(tx.config)
	tx.ScheduledTask = NewScheduledTaskClient(tx.config)
	tx.ScheduledTaskLog = NewScheduledTaskLogClient(tx.config)
	tx.SensitiveWord = NewSensitiveWordClient(tx.config)
	tx.SensitiveWordHit = NewSensitiveWordHitClient(tx.config)
	tx.SiteNotification = NewSiteNotificationClient(tx.config)
	tx.Tag = NewTagClient(tx.config)
	tx.User = NewUserClient(tx.config)
}

// txDriver wraps the given dialect.Tx with a nop dialect.Driver implementation.
// The idea is to support transactions without adding any extra code to the builders.
// When a builder calls to driver.Tx(), it gets the same dialect.Tx instance.
// Commit and Rollback are nop for the internal builders and the user must call one
// of them in order to commit or rollback the transaction.
//
// If a closed transaction is embedded in one of the generated entities, and the entity
// applies a query, for example: Article.QueryXXX(), the query will be executed
// through the driver which created this transaction.
//
// Note that txDriver is not goroutine safe.
type txDriver struct {
	// the driver we started the transaction from.
	drv dialect.Driver
	// tx is the underlying transaction.
	tx dialect.Tx
	// completion hooks.
	mu         sync.Mutex
	onCommit   []CommitHook
	onRollback []RollbackHook
}

// newTx creates a new transactional driver.
func newTx(ctx context.Context, drv dialect.Driver) (*txDriver, error) {
	tx, err := drv.Tx(ctx)
	if err != nil {
		return nil, err
	}
	return &txDriver{tx: tx, drv: drv}, nil
}

// Tx returns the transaction wrapper (txDriver) to avoid Commit or Rollback calls
// from the internal builders. Should be called only by the internal builders.
func (tx *txDriver) Tx(context.Context) (dialect.Tx, error) { return tx, nil }

// Dialect returns the dialect of the driver we started the transaction from.
func (tx *txDriver) Dialect() string { return tx.drv.Dialect() }

// Close is a nop close.
func (*txDriver) Close() error { return nil }

// Commit is a nop commit for the internal builders.
// User must call `Tx.Commit` in order to commit the transaction.
func (*txDriver) Commit() error { return nil }

// Rollback is a nop rollback for the internal builders.
// User must call `Tx.Rollback` in order to rollback the transaction.
func (*txDriver) Rollback() error { return nil }

// Exec calls tx.Exec.
func (tx *txDriver) Exec(ctx context.Context, query string, args, v any) error {
	return tx.tx.Exec(ctx, query, args, v)
}

// Query calls tx.Query.
func (tx *txDriver) Query(ctx context.Context, query string, args, v any) error {
	return tx.tx.Query(ctx, query, args, v)
}

var _ dialect.Driver = (*txDriver)(nil)

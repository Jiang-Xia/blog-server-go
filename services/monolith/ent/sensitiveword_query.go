// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Jiang-Xia/blog-server-go/services/monolith/ent/predicate"
	"github.com/Jiang-Xia/blog-server-go/services/monolith/ent/sensitiveword"
)

// SensitiveWordQuery is the builder for querying SensitiveWord entities.
type SensitiveWordQuery struct {
	config
	ctx        *QueryContext
	order      []sensitiveword.OrderOption
	inters     []Interceptor
	predicates []predicate.SensitiveWord
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SensitiveWordQuery builder.
func (swq *SensitiveWordQuery) Where(ps ...predicate.SensitiveWord) *SensitiveWordQuery {
	swq.predicates = append(swq.predicates, ps...)
	return swq
}

// Limit the number of records to be returned by this query.
func (swq *SensitiveWordQuery) Limit(limit int) *SensitiveWordQuery {
	swq.ctx.Limit = &limit
	return swq
}

// Offset to start from.
func (swq *SensitiveWordQuery) Offset(offset int) *SensitiveWordQuery {
	swq.ctx.Offset = &offset
	return swq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (swq *SensitiveWordQuery) Unique(unique bool) *SensitiveWordQuery {
	swq.ctx.Unique = &unique
	return swq
}

// Order specifies how the records should be ordered.
func (swq *SensitiveWordQuery) Order(o ...sensitiveword.OrderOption) *SensitiveWordQuery {
	swq.order = append(swq.order, o...)
	return swq
}

// First returns the first SensitiveWord entity from the query.
// Returns a *NotFoundError when no SensitiveWord was found.
func (swq *SensitiveWordQuery) First(ctx context.Context) (*SensitiveWord, error) {
	nodes, err := swq.Limit(1).All(setContextOp(ctx, swq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sensitiveword.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (swq *SensitiveWordQuery) FirstX(ctx context.Context) *SensitiveWord {
	node, err := swq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SensitiveWord ID from the query.
// Returns a *NotFoundError when no SensitiveWord ID was found.
func (swq *SensitiveWordQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = swq.Limit(1).IDs(setContextOp(ctx, swq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sensitiveword.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (swq *SensitiveWordQuery) FirstIDX(ctx context.Context) int {
	id, err := swq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SensitiveWord entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SensitiveWord entity is found.
// Returns a *NotFoundError when no SensitiveWord entities are found.
func (swq *SensitiveWordQuery) Only(ctx context.Context) (*SensitiveWord, error) {
	nodes, err := swq.Limit(2).All(setContextOp(ctx, swq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sensitiveword.Label}
	default:
		return nil, &NotSingularError{sensitiveword.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (swq *SensitiveWordQuery) OnlyX(ctx context.Context) *SensitiveWord {
	node, err := swq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SensitiveWord ID in the query.
// Returns a *NotSingularError when more than one SensitiveWord ID is found.
// Returns a *NotFoundError when no entities are found.
func (swq *SensitiveWordQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = swq.Limit(2).IDs(setContextOp(ctx, swq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sensitiveword.Label}
	default:
		err = &NotSingularError{sensitiveword.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (swq *SensitiveWordQuery) OnlyIDX(ctx context.Context) int {
	id, err := swq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SensitiveWords.
func (swq *SensitiveWordQuery) All(ctx context.Context) ([]*SensitiveWord, error) {
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryAll)
	if err := swq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SensitiveWord, *SensitiveWordQuery]()
	return withInterceptors[[]*SensitiveWord](ctx, swq, qr, swq.inters)
}

// AllX is like All, but panics if an error occurs.
func (swq *SensitiveWordQuery) AllX(ctx context.Context) []*SensitiveWord {
	nodes, err := swq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SensitiveWord IDs.
func (swq *SensitiveWordQuery) IDs(ctx context.Context) (ids []int, err error) {
	if swq.ctx.Unique == nil && swq.path != nil {
		swq.Unique(true)
	}
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryIDs)
	if err = swq.Select(sensitiveword.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (swq *SensitiveWordQuery) IDsX(ctx context.Context) []int {
	ids, err := swq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (swq *SensitiveWordQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryCount)
	if err := swq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, swq, querierCount[*SensitiveWordQuery](), swq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (swq *SensitiveWordQuery) CountX(ctx context.Context) int {
	count, err := swq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (swq *SensitiveWordQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryExist)
	switch _, err := swq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (swq *SensitiveWordQuery) ExistX(ctx context.Context) bool {
	exist, err := swq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SensitiveWordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (swq *SensitiveWordQuery) Clone() *SensitiveWordQuery {
	if swq == nil {
		return nil
	}
	return &SensitiveWordQuery{
		config:     swq.config,
		ctx:        swq.ctx.Clone(),
		order:      append([]sensitiveword.OrderOption{}, swq.order...),
		inters:     append([]Interceptor{}, swq.inters...),
		predicates: append([]predicate.SensitiveWord{}, swq.predicates...),
		// clone intermediate query.
		sql:  swq.sql.Clone(),
		path: swq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Word string `json:"word,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SensitiveWord.Query().
//		GroupBy(sensitiveword.FieldWord).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (swq *SensitiveWordQuery) GroupBy(field string, fields ...string) *SensitiveWordGroupBy {
	swq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SensitiveWordGroupBy{build: swq}
	grbuild.flds = &swq.ctx.Fields
	grbuild.label = sensitiveword.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Word string `json:"word,omitempty"`
//	}
//
//	client.SensitiveWord.Query().
//		Select(sensitiveword.FieldWord).
//		Scan(ctx, &v)
func (swq *SensitiveWordQuery) Select(fields ...string) *SensitiveWordSelect {
	swq.ctx.Fields = append(swq.ctx.Fields, fields...)
	sbuild := &SensitiveWordSelect{SensitiveWordQuery: swq}
	sbuild.label = sensitiveword.Label
	sbuild.flds, sbuild.scan = &swq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SensitiveWordSelect configured with the given aggregations.
func (swq *SensitiveWordQuery) Aggregate(fns ...AggregateFunc) *SensitiveWordSelect {
	return swq.Select().Aggregate(fns...)
}

func (swq *SensitiveWordQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range swq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, swq); err != nil {
				return err
			}
		}
	}
	for _, f := range swq.ctx.Fields {
		if !sensitiveword.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if swq.path != nil {
		prev, err := swq.path(ctx)
		if err != nil {
			return err
		}
		swq.sql = prev
	}
	return nil
}

func (swq *SensitiveWordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SensitiveWord, error) {
	var (
		nodes = []*SensitiveWord{}
		_spec = swq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SensitiveWord).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SensitiveWord{config: swq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, swq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (swq *SensitiveWordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := swq.querySpec()
	_spec.Node.Columns = swq.ctx.Fields
	if len(swq.ctx.Fields) > 0 {
		_spec.Unique = swq.ctx.Unique != nil && *swq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, swq.driver, _spec)
}

func (swq *SensitiveWordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sensitiveword.Table, sensitiveword.Columns, sqlgraph.NewFieldSpec(sensitiveword.FieldID, field.TypeInt))
	_spec.From = swq.sql
	if unique := swq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if swq.path != nil {
		_spec.Unique = true
	}
	if fields := swq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sensitiveword.FieldID)
		for i := range fields {
			if fields[i] != sensitiveword.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := swq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := swq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := swq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := swq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (swq *SensitiveWordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(swq.driver.Dialect())
	t1 := builder.Table(sensitiveword.Table)
	columns := swq.ctx.Fields
	if len(columns) == 0 {
		columns = sensitiveword.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if swq.sql != nil {
		selector = swq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if swq.ctx.Unique != nil && *swq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range swq.predicates {
		p(selector)
	}
	for _, p := range swq.order {
		p(selector)
	}
	if offset := swq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := swq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SensitiveWordGroupBy is the group-by builder for SensitiveWord entities.
type SensitiveWordGroupBy struct {
	selector
	build *SensitiveWordQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (swgb *SensitiveWordGroupBy) Aggregate(fns ...AggregateFunc) *SensitiveWordGroupBy {
	swgb.fns = append(swgb.fns, fns...)
	return swgb
}

// Scan applies the selector query and scans the result into the given value.
func (swgb *SensitiveWordGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, swgb.build.ctx, ent.OpQueryGroupBy)
	if err := swgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SensitiveWordQuery, *SensitiveWordGroupBy](ctx, swgb.build, swgb, swgb.build.inters, v)
}

func (swgb *SensitiveWordGroupBy) sqlScan(ctx context.Context, root *SensitiveWordQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(swgb.fns))
	for _, fn := range swgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*swgb.flds)+len(swgb.fns))
		for _, f := range *swgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*swgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := swgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SensitiveWordSelect is the builder for selecting fields of SensitiveWord entities.
type SensitiveWordSelect struct {
	*SensitiveWordQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sws *SensitiveWordSelect) Aggregate(fns ...AggregateFunc) *SensitiveWordSelect {
	sws.fns = append(sws.fns, fns...)
	return sws
}

// Scan applies the selector query and scans the result into the given value.
func (sws *SensitiveWordSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sws.ctx, ent.OpQuerySelect)
	if err := sws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SensitiveWordQuery, *SensitiveWordSelect](ctx, sws.SensitiveWordQuery, sws, sws.inters, v)
}

func (sws *SensitiveWordSelect) sqlScan(ctx context.Context, root *SensitiveWordQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sws.fns))
	for _, fn := range sws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

# Plan {NN}：{标题}

> 对应计划：[`.cursor/plans/{NN}-{计划文件名}.md`](../.cursor/plans/{NN}-{计划文件名}.md)
>
> **交付日期**：YYYY-MM-DD  
> **架构形态**：模块化单体 / 4 微服务

## 交付摘要

（1–3 句话说明本阶段完成了什么、对前端的可见影响。）

## 目录与模块

（本阶段新增或变更的关键路径，表格或树形均可。）

| 路径 | 职责 |
|------|------|
| `services/monolith/internal/...` | … |

## 配置与环境

（新增配置项、环境变量、`configs/*.yaml` 段落说明。）

## 接口一览

（本阶段对外 REST / gRPC / WS 端点；路径、方法、简要说明。无业务接口可写「无新增业务接口」。）

## 本地启动与验收

```bash
# 启动
make dev

# 冒烟（替换为本阶段实际命令）
curl -sf http://localhost:5000/health
```

## 与 NestJS 差异

（行为、字段、错误码、路径等有意或无意差异；无则写「无」。）

## 已知限制与后续

（本计划「不做」范围内、留给下一计划的债、风险。）

## 验收勾选

- [ ] 计划内任务清单已全部完成
- [ ] 可脚本化验收已通过
- [ ] 本文档已写入 `docs/`
- [ ] [`docs/README.md`](./README.md) 索引状态已更新

# Plan 03：博客内容域（模块化单体）

## 元信息

| 项 | 内容 |
|----|------|
| **目标** | 迁移博客核心业务模块，C 端 + admin 内容管理全功能可用 |
| **前置依赖** | [02-用户与认证域.md](./02-用户与认证域.md) 验收通过（依赖 `UserService` interface） |
| **周期** | ~4-5 周 |
| **架构形态** | 模块化单体（`internal/blog/` 包） |
| **原方案章节** | 三（3.1 blog-service、3.3 表归属）、五（5.6 Ent）、8.3 第3-7/9周 |

## 模块范围

对应未来 **blog-service**（WebSocket 在 Plan 04 实现）。

### NestJS 对照

| 批次 | NestJS 路径 |
|------|-------------|
| 内容 | `features/article/`、`category/`、`tag/` |
| 互动 | `features/comment/`、`reply/`、`like/`、`collect/` |
| 周边 | `features/msgboard/`、`link/`、`file/`、`email/`、`resources/` |
| 系统 | `features/notification/`、`operation-log/`、`scheduled-task/` |

### 负责表

`article`, `category`, `tag`, `article_tags_tag`, `comment`, `reply`, `like`, `collect`, `msgboard`, `link`, `resources`, `file`, `notification`, `operation_log`

## API 路径（保持兼容）

| 路径 | 说明 |
|------|------|
| `/api/v1/article/*` | 文章 CRUD、列表、详情、搜索 |
| `/api/v1/category/*` | 分类 |
| `/api/v1/tag/*` | 标签 |
| `/api/v1/comment/*` | 评论 |
| `/api/v1/reply/*` | 回复 |
| `/api/v1/like/*` | 点赞 |
| `/api/v1/collect/*` | 收藏 |
| `/api/v1/msgboard/*` | 留言板 |
| `/api/v1/link/*` | 友链 |
| `/api/v1/file/*` | 文件上传 |
| `/api/v1/notification/*` | 通知（CRUD，推送在 Plan 04） |

## 周次计划（对照原方案 8.3）

| 周 | 模块 | 任务 |
|----|------|------|
| 1-2 | article + category + tag | CRUD、关联、分页、Markdown 渲染（goldmark） |
| 3-4 | comment + reply + like + collect | 互动逻辑、敏感词过滤（调 user 模块） |
| 5 | msgboard + link + file + email | 留言板、友链、multipart 上传、gomail |
| 6 | notification + operation-log + scheduler | 通知 CRUD、操作日志、cron 框架接入 |

## 关键实现要点

### 跨模块调用（不跨表 JOIN）

```go
// internal/blog/service/article.go
func (s *ArticleService) Detail(ctx context.Context, id uint64) (*ArticleDetailDTO, error) {
    article, err := s.repo.GetByID(ctx, id)
    // 作者信息走 UserService interface，不查 user 表
    author, err := s.userSvc.GetUser(ctx, article.AuthorID)
    return &ArticleDetailDTO{Article: article, Author: author}, nil
}
```

### Ent repo 模式

- `internal/blog/repo/` — 仅访问 blog 域表
- `internal/blog/domain/` — 领域模型
- `internal/blog/service/` — 用例逻辑
- `internal/blog/handler/` — Hertz HTTP handler

### 文件上传

- Hertz multipart，存储路径与 NestJS 一致
- 静态资源访问路径保持 `/uploads/*` 或现有 CDN 规则

### 定时任务（骨架）

- `robfig/cron/v3` 接入
- 本阶段注册空 job 或简单清理任务；RPG/通知相关 cron 在 Plan 04 完善

## 任务清单

### 第 1-2 周：内容模块

- [ ] article CRUD + 软删除 + 置顶/推荐
- [ ] category 树形 CRUD
- [ ] tag CRUD + article-tag 多对多
- [ ] 文章列表：分页、分类/标签筛选、关键词搜索
- [ ] 文章详情：Markdown 渲染、阅读量统计
- [ ] admin 文章管理接口

### 第 3-4 周：互动模块

- [ ] comment CRUD + 审核
- [ ] reply 嵌套回复
- [ ] like 点赞/取消（幂等）
- [ ] collect 收藏/取消
- [ ] 敏感词过滤接入 comment/msgboard

### 第 5 周：周边模块

- [ ] msgboard 留言板
- [ ] link 友链 CRUD
- [ ] file 上传/删除
- [ ] email 发送（注册验证、通知邮件等）
- [ ] resources 资源管理（若有）

### 第 6 周：通知 + 日志 + cron

- [ ] notification CRUD + 分页列表
- [ ] operation-log 记录 admin 操作
- [ ] cron 框架接入，注册占位 job
- [ ] C 端 + admin 全功能冒烟测试

## 验收标准

- [ ] blog-home-nuxt 首页、文章详情、评论、点赞、收藏功能正常
- [ ] blog-admin 文章/分类/标签/评论管理可用
- [ ] 作者信息正确展示（通过 UserService interface）
- [ ] 文件上传与 NestJS 路径兼容
- [ ] 错误码与原项目一一对应
- [ ] 无跨域表 JOIN（code review 确认）

## 生产切换提示

本计划完成后可灰度切换博客相关路径：

```
/api/v1/article  → Go
/api/v1/category → Go
/api/v1/comment  → Go
...（其余 blog 域路径）
```

## 风险与注意点

| 风险 | 对策 |
|------|------|
| Markdown XSS | 对照 NestJS marked 配置，goldmark 启用安全选项 |
| 文件路径不一致 | 迁移 uploads 目录或 Nginx alias 对齐 |
| 点赞/收藏并发 | 数据库唯一索引 + 幂等处理 |

## 下一步

完成验收后进入 [04-实时通信与RPG支付.md](./04-实时通信与RPG支付.md)。

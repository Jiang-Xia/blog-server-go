---
description: 执行计划完成后须在 docs/ 写入对应交付文档
alwaysApply: true
---

# 计划交付文档（强制）

完成 [`.cursor/plans/`](../plans/) 中任一执行计划（01–10）的**验收**后，必须在 [`docs/`](../../docs/) 写入对应交付文档，再开始下一计划。

## 命名与索引

| 计划文件 | 交付文档 |
|----------|----------|
| `.cursor/plans/01-脚手架与公共基础.md` | `docs/01-脚手架与公共基础.md` |
| … | `docs/{同序号与标题}.md` |

完整映射见 [`docs/README.md`](../../docs/README.md)。

## 文档内容（最低要求）

1. **交付摘要**：本阶段实际完成范围
2. **目录与模块**：新增/变更的关键路径
3. **配置与环境**：`configs/`、环境变量
4. **接口一览**：本阶段对外 REST / gRPC / WS（无则明示）
5. **本地启动与验收**：可复制命令（`make dev`、`curl` 等）
6. **与 NestJS 差异**：路径、字段、错误码等
7. **已知限制与后续**：留给下一计划的边界

可复制 [`docs/_template.md`](../../docs/_template.md) 起草。

## Agent 交付自检

1. 对应 `docs/{NN}-*.md` 已创建或更新
2. `docs/README.md` 索引表该计划状态改为「已交付」
3. 计划文件「文档交付」小节 `- [x] 文档已写入 docs/` 已勾选
4. 回复用户时列出已改 `docs/` 路径

## 与计划文件的关系

- **`.cursor/plans/`**：任务清单、验收标准、边界（执行前读）
- **`docs/`**：实现结果、命令、差异（验收后写）

勿只勾选计划任务而不写 `docs/`；勿在 `docs/` 重复粘贴整份计划。

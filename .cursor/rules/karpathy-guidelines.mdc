---
description: 编码行为准则——最小改动、先澄清、可验证交付（与 hertz-* 项目规则互补）
alwaysApply: true
---

# 编码行为准则

减少 LLM 常见过度实现问题；与 hertz-* 项目规则互补，冲突时以项目规则为准。

## 1. 先想后写

- 不确定时先说明假设或提问，不要静默选方案
- 有更简单做法时主动提出；需求不清时停下来点名困惑点

## 2. 简单优先

- 只写解决当前问题的最少代码；不加未要求的功能、抽象、配置项
- 不写不可能场景的 error handling；200 行能 50 行搞定就重写

## 3. 精准改动

- 只改任务相关代码；不顺手「优化」相邻代码、注释或格式
- 匹配现有风格；无关 dead code 只提及不删除
- 自己改动产生的 orphan import/变量要清理；不删改动前就存在的 dead code

## 4. 目标驱动

- 把任务转为可验证目标：`修复 bug` → 先复现再验证通过
- 多步任务简要列计划：`步骤 → 验证方式`

**检验标准：** diff 中每行改动都能追溯到用户请求；澄清问题出现在实现之前而非犯错之后。
